apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kimmi.fullname" . }}-test-connection"
  labels:
{{ include "kimmi.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kimmi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
