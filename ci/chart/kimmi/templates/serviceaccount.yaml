{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "kimmi.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "kimmi.labels" . | indent 4 }}
{{- end -}}
